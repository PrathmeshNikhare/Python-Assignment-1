# Python-Assignment-1
This repository contains assignment 1 from the TuteDude's python course.

# Task-1
>> In this task first the program takes input of any two numbers through input() function in python.
>> These two numbers are stored in two variables 'a' and 'b' respectively.
>> Then we can simultaneously perform the arithmetic operations on these two numbers using arithmetic operators in python and print them using the print() function.

# Task-2
>> In this task the program takes input of two strings as fist name and last name through input() function.
>> These two strings are then concatenated using '+' operator in a variable fullname.
>> Then we use the print() to display our required output. 
